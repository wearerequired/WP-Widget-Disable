# Default task
default:
- 'checktextdomain'
- 'replace:header'
- 'replace:plugin'

# Build task
build:
- 'default'
- 'clean'
- 'copy:main'
- 'compress' # Can comment this out for WordPress.org plugins
